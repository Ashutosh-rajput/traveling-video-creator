[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/api/*"
  to = "https://traveling-video-creator-production.up.railway.app/:splat"
  status = 200
  force = true

# Replace the `to` target with your backend URL if needed.
